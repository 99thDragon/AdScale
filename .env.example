# Dev: Vite serves a mock POST /campaigns/generate (default — no backend needed)
# VITE_USE_REAL_API=true

# When using the real backend (requires API on localhost:8000):
# VITE_USE_REAL_API=true

# Production / custom backend host:
# VITE_API_BASE_URL=https://api.example.com
